import { AccountExistValidator } from './account-exist.validator';

describe('AccountExistValidator', () => {
  it('should create an instance', () => {
    expect(new AccountExistValidator()).toBeTruthy();
  });
});
